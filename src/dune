(executable
 (name swhids)
 (modules swhids)
 (instrumentation
  (backend bisect_ppx))
 (libraries swhids))

(library
 (name swhids)
 (modules compute lang lexer menhir_parser parser pp utils)
 (private_modules menhir_parser)
 (libraries digestif))

(ocamllex
 (modules lexer))

(menhir
 (modules menhir_parser))
