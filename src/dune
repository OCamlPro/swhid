(library
 (public_name swhid_common)
 (modules fcompute git lang))

(library
 (public_name swhid)
 (modules compute download json lexer menhir_parser parse pp)
 (private_modules git json lexer menhir_parser)
 (libraries swhid_common digestif ezcurl jsonm)
 (instrumentation
  (backend bisect_ppx)))

(ocamllex
 (modules lexer))

(menhir
 (modules menhir_parser))
